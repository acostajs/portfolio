{
  "name": "portfolio",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:frontend": "cd frontend && bun dev",
    "dev:backend": "cd backend && uv run uvicorn main:app --reload",
    "setup:backend": "cd backend && uv sync",
    "build": "bun install && bun run --cwd frontend build",
    "lint": "bun run lint:frontend && bun run lint:backend",
    "lint:frontend": "cd frontend && bun run lint && bun tsc --noEmit",
    "lint:backend": "cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright",
    "test": "bun run test:frontend && bun run test:backend",
    "test:frontend": "cd frontend && bun test",
    "test:backend": "cd backend && uv run pytest",
    "format": "prettier --write . && cd backend && uv run ruff format .",
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "cd frontend && bun run lint",
      "cd frontend && bun tsc --noEmit",
      "prettier --write"
    ],
    "frontend/**/*.{js,jsx}": [
      "cd frontend && bun run lint",
      "prettier --write"
    ],
    "backend/**/*.py": [
      "cd backend && uv run ruff check --fix",
      "cd backend && uv run ruff format",
      "cd backend && uv run pyright"
    ],
    "**/*.{json,md,css}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.8.3"
  }
}
